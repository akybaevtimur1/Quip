"""Чистые стадии пайплайна (вход-файл → выход-файл, без HTTP/DB).

Каждый ``stageN_*.py`` — pure-функции: их можно гонять в unit-тестах без сети.
Склейка стадий живёт в ``app/run.py`` и ``app/tasks.py``, НЕ здесь.
"""
