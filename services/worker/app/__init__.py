"""ClipFlow worker package (FastAPI + pure pipeline).

Имя импорта пакета — ``app`` (дистрибутив на pip/uv — ``clipflow-worker``).
Единственный источник версии для API — ``__version__`` ниже.
"""

__version__ = "0.1.0"
