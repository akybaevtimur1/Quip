# 🌙 Night Audit Report — 2026-06-15

Автономная ночная сессия: рой из **15 агентов-дебагеров по ролям** + независимый
ревью-агент прошли по всему продукту (бэкенд `services/worker`, фронт `apps/web`,
деплой `deploy/modal`), нашли и починили баги. Подробные пер-доменные отчёты —
`docs/night-audit/*.md`. Координация по матрице непересекающихся файлов (агенты не
топтались друг по другу), верификация и коммиты — поволново оркестратором.

## Гейт
- **Baseline на старте:** `just check` зелёный, 472 теста.
- **После аудита:** `just check` зелёный, **~520+ тестов** (агенты добавили ~70 на TDD).
- 5 коммитов: `8a819f2`, `191816e`, `a6c8927`, `ecfb791`, `9a07bf4` (+ финальная полировка).
- Независимый ревью кумулятивного диффа: **все 7 высоко-рисковых зон корректны, регрессий нет**
  (`docs/night-audit/REVIEW.md`). Инвариант reframe fps-grid (Δ=0) НЕ тронут.

## Самое важное, что починено (по severity)

### 🔴 Критическое
- **PAYG-кредиты никогда не списывались + двойной учёт минут** (биллинг). `check_quota`
  считал split «месячное/PAYG», но метеринг писал только месячный счётчик и не трогал
  `profiles.payg_credits` → платные кредиты бесконечны, а их минуты ещё и засчитывались
  в месячный лимит (двойной заряд). Полностью переписан учёт (billing/db/supa/tasks),
  +17 тестов. Дормантно до `BILLING_ENABLED`, но теперь корректно ДО запуска биллинга.

### 🟠 High (ломало пользователя/деплой)
- **Петля редиректа на логин** ×2: (1) `auth/callback` тихо глотал ошибку истёкшей
  PKCE-ссылки → /dashboard → гейт → /login по кругу; (2) `proxy.ts` терял ротированные
  session-cookies при редиректе → ложный выброс залогиненного юзера.
- **Бесконечный спиннер**: `getJob` без таймаута — зависший воркер (Modal cold start)
  навсегда оставлял UI в «tracking». Теперь 15с AbortController → честная «потеря связи».
- **Ложный 409→полный reload редактора** после trim/кадр/аспект (устаревший `editRef`).
- **Тихая потеря текста субтитров**: `{laughs}` и т.п. рендерились ПУСТО (libass парсил
  как тег) и в превью, и в экспорте. Добавлено ASS-экранирование (проверено рендерами).
- **Modal: краш деплоя** если нет cookies-файла (`YTDLP_COOKIES_FILE` всегда выставлялся).
- **Gemini-ретрай**: битый ключ/404/400 хаммерились ~2 мин вместо fail-fast.
- **`parse_fps` пропускал fps≤0** → `ZeroDivisionError` далеко на рендере.

### 🟡 Medium / прочее
- `apply_trim` 500 на пустом вводе → JobError(400); валидация edge/overlap в редакторе.
- Пустой результат AI-глав → `failed` с причиной (был тихий пустой «done»).
- Провайдер-aware ключ кэша транскрипции; `dispatch.spawn` сбой → джоб `failed` (не висит).
- Supabase `set_user_plan/add_payg_credits` → upsert (оплата не терялась на гонке профиля).
- `format`: часы для >60мин видео + NaN-гварды; sitemap noindex/pricing; faq money-copy
  (Free cap 30→60 мин, «90мин=2кредита» → пропорционально 1.5); Footer мёртвые якоря;
  Select `peer-focus`.

`plans.ts ↔ billing.py` — **дрейф цен НОЛЬ** (проверено число за числом).

---

## ⚠️ Открыто — требует РЕШЕНИЯ/ДЕЙСТВИЯ фаундера (не починено агентами осознанно)

### Перед запуском биллинга
1. ✅✅ **СДЕЛАНО + ПРИМЕНЕНО В ПРОДЕ** — идемпотентность usage в коде (check-then-act по
   `job_id`, SQLite+Supabase) И миграция `0003` **применена к боевому Supabase** (проект
   `qiagetbnsssvbiowuxpp`, индекс `usage_events_job_id_key` создан; 0002 уже была на месте,
   схема jobs/profiles/usage_events/clip_edits подтверждена). Делать тебе тут больше нечего.

### Прод-деплой (Modal)
2. ✅ **СДЕЛАНО (upload на Modal больше не виснет)** — `create_upload_job` на Modal
   (`MODAL_SPAWN=1`) теперь стейджит исходник в R2 (`storage.upload_source`) и спавнит новую
   долгоживущую `upload_job` (она качает исходник на свой контейнер → `run_upload_job`), как
   YouTube-путь. Локально без изменений (BackgroundTask). +2 теста на ветку диспатча.
   ✅✅ **ЗАДЕПЛОЕНО В ПРОД** — `modal deploy` выполнен (аккаунт `akybaevtimur7`), функции
   `web`/`run_job`/`upload_job`/`render_job` живут, web отвечает 200 на /healthz
   (`https://akybaevtimur7--quip-worker-web.modal.run`). Весь ночной код теперь в проде.
   ⚠️ Остаётся ОДНА твоя проверка: прогнать e2e-загрузку файла через UI (R2-раундтрип upload_job
   проверяется только живым прогоном; стоит ~$0.05 Deepgram — поэтому не запускал сам без спроса).

### Что осталось чисто за тобой (нужны дашборды/решения, MCP/CLI не покрывают)
- **`BILLING_ENABLED`** на Modal НЕ включал намеренно: включение начнёт гейтить квоты (402) для
  юзеров без профиля и требует настроенного Polar-вебхука. Это твоё продуктовое решение.
- **Polar**: `POLAR_ACCESS_TOKEN`/`POLAR_SERVER`/`POLAR_WEBHOOK_SECRET` (дашборд Polar) — для
  живого checkout/вебхука.
- **Supabase Auth → Email-шаблон OTP** (`{{ .Token }}`) ИЛИ сменить UI регистрации на «ссылка»
  (FE-B): шаблоны писем MCP не правит — это дашборд Supabase Auth.
- **Фронт `NEXT_PUBLIC_WORKER_URL`** = `https://akybaevtimur7--quip-worker-web.modal.run` на том
  проекте, что реально собирает `apps/web` (см. заметку: видимый Vercel-проект — лендинг-репо).
3. **Modal billing/storage env-десинхрон**: `STORAGE_BACKEND=r2` без `BILLING_ENABLED` →
   джобы в Postgres, а биллинг в эфемерный SQLite → usage/plan/PAYG теряются на scale-to-zero.
   Ставить оба флага вместе.
4. **AI-главы платят Gemini на каждом cold-контейнере** (кэш на scratch-диске) → в Postgres.
5. **run_job** `timeout=3600` без явного `memory=` → риск OOM/timeout на длинных Pro-видео;
   нагрузить и выставить `memory=`. `bgutil-pot-provider` не в Modal-образе (риск YouTube-гейта).

### Конфиг сервисов
6. **AuthForm OTP vs ссылка** (FE-B): шаг «code» ждёт цифровой OTP, но Supabase шлёт
   ссылку-подтверждение, если шаблон письма не использует `{{ .Token }}`. Либо настроить
   OTP-шаблон в Supabase, либо сменить UI на «проверьте письмо со ссылкой».

### Низкий / опционально
7. Усиление контракта `models.py` (ExtendBody.edge→Literal, TrimBody min_length) — поведение
   уже корректно (JobError→400), это лишь сдвиг отказа на слой валидации (422). Опционально.
8. BE-F: PostgREST `eq.{value}` — низкая эксплуатируемость (UUID/серверные id); verify_signature
   принимает любой scheme-префикс (косметика). FE-A: ProofStrip `<dd>` перед `<dt>` (a11y-нит).
   FE-C: гонка очереди мутаций в момент ‹/› навигации (узкое окно). FE-D: mock игнорит max_clips.
9. **Безопасность (из HANDOFF, не ночь):** youtube-куки в истории git (`59d07b4`) — перевыпустить.

---

*Метод: каждый агент — расследование + фикс в строгих границах файлов, TDD на pure-логике,
без коммитов; оркестратор серилизовал `just check` + коммиты поволново; кросс-доменные
инварианты (PAYG) оркестратор чинил сам. Источник правил — `docs/night-audit/README.md`.*
