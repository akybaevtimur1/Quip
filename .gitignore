# ── Node / Next.js ──
node_modules/
.next/
out/
build/
.turbo/
.pnpm-store/
*.tsbuildinfo
next-env.d.ts

# ── Python / uv ──
.venv/
__pycache__/
*.py[cod]
.pytest_cache/
.mypy_cache/
.ruff_cache/
*.egg-info/

# ── Secrets / env (примеры коммитим) ──
.env
.env.local
.env.*.local

# ── Cookies (YouTube / auth — НИКОГДА не коммитим, только по env-var) ──
cookies.json
cookies*.txt
*_cookies.txt
*_cookies.json

# ── DB / состояние ──
*.db
*.db-journal
*.sqlite

# ── Артефакты воркера (Phase 0 = local-fs) ──
services/worker/data/
services/worker/tmp/

# ── Видео-фикстуры: golden JSON коммитим, видео — НЕТ ──
tests/fixtures/**/*.mp4
tests/fixtures/**/*.mkv
tests/fixtures/**/*.webm
services/worker/tests/fixtures/**/*.mp4
services/worker/tests/fixtures/**/*.mkv
services/worker/tests/fixtures/**/*.webm

# ── ML-модели (качаются в кэш при первом использовании) ──
*.tflite

# ── Веб: мок-видео (тяжёлые, не коммитим) + временные превью/браузер ──
apps/web/public/mock/
palettes_preview.png
ui_*.png
.playwright-mcp/

# ── Brainstorming visual-companion мокапы (локальные, не коммитим) ──
.superpowers/

# ── Логи (включая чужие debug.log от Electron/Chromium) ──
*.log

# ── OS / редакторы ──
.DS_Store
Thumbs.db
.idea/
*.swp
.gstack/
