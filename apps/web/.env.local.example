# Скопируй в apps/web/.env.local чтобы web ходил в РЕАЛЬНЫЙ воркер (а не во встроенный мок).
# Без этого файла фронт использует /api/mock (удобно для разработки UI без бэка).
NEXT_PUBLIC_WORKER_URL=http://localhost:8000
